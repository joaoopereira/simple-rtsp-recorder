{
  "name": "simple-rtsp-recorder",
  "version": "1.3.1",
  "main": "index.js",
  "scripts": {
    "start": "cross-env ENV_FILE=.env node server.js",
    "prod": "cross-env ENV_FILE=prod.env node server.js",
    "pack": "rimraf dist && pkg . && cp -r install-scripts-win/* dist",
    "bump": "commit-and-tag-version && git push --follow-tags",
    "clean": "rimraf dist && rimraf node_modules && rm package-lock.json"
  },
  "bin": {
    "simple-rtsp-recorder": "server.js"
  },
  "pkg": {
    "scripts": "server.js",
    "assets": [
      "public/**/*"
    ],
    "targets": [
      "latest-win-x64"
    ],
    "outputPath": "dist"
  },
  "keywords": [
    "rtsp",
    "recorder"
  ],
  "author": "joaoopereira",
  "license": "None",
  "description": "simple web server to record file from an rtsp camera stream",
  "dependencies": {
    "dotenv": "17.2.3",
    "express": "5.2.1",
    "fluent-ffmpeg": "^2.1.3",
    "winston": "^3.19.0"
  },
  "devDependencies": {
    "@yao-pkg/pkg": "^6.12.0",
    "commit-and-tag-version": "12.6.1",
    "cross-env": "10.1.0",
    "rimraf": "^6.1.2"
  }
}
